Following files are neccessary for compilation (provided by Microsoft so I can't distribute them):
hidpi.h, hidsdi.h, hidusage.h, setupapi.h, hid.lib
and can be found in Windows Driver Kit (free to download from MSDN) or via Google

libmcrypt.dll needs to be in runing path of resulting binary
